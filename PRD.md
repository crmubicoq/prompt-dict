# PRD — 프롬프트 전자사전 (prompt-dict)

> 작성 기준: 라이언 카슨 3단계 규칙 중 1단계
> 다음 단계: `PROGRESS.md` (Task List) → 실행

---

## 1. 한 줄 정의

교육지원팀이 **잘 만든 프롬프트를 잃어버리지 않고, 서로 찾아 쓰고, 개선 과정을 남기는** 팀 공용 사전.

---

## 2. 대상 사용자

| 구분 | 내용 |
|---|---|
| 주 사용자 | 교육지원팀 내부 구성원 |
| 규모 | 소규모 (10명 내외 가정) |
| 기술 수준 | 비개발자 중심. 설치·설정 없이 브라우저로 접근 |
| 접근 경로 | 팀 허브(8801) → 카드 클릭 |

**대상 아님:** 회사 전체 공개, 외부 사용자, API 소비자

---

## 3. 해결하려는 문제

사용자가 직접 지목한 통증 3가지.

### P-1. 프롬프트를 어디 뒀는지 못 찾는다
메모장·카톡·문서 여기저기 흩어져 있어, 예전에 잘 썼던 프롬프트를 다시 쓰려면 찾는 데 시간이 든다.
→ **검색이 이 제품의 심장.** 검색이 안 되면 존재 이유가 없음.

### P-2. 팀원마다 각자 보관해서 공유가 안 된다
A가 고생해서 다듬은 프롬프트를 B가 모르고 처음부터 다시 만든다.
→ **서버 배포 자체로 대부분 해결.** 별도의 정교한 공유 기능은 불필요.

### P-3. 좋은 프롬프트의 개선 이력이 안 남는다
프롬프트는 한 번에 완성되지 않고 여러 번 고치며 좋아지는데, 덮어쓰면 이전 버전이 사라진다.
"저번 버전이 더 나았던 것 같은데" 를 확인할 방법이 없다.
→ **버전 이력 + 되돌리기가 차별 기능.**

### P-4. 등록이 번거로워서 애초에 쌓지 못한다 ★ 최대 병목
도구를 만들고도 데이터를 넣지 못한 채 방치됨(샘플 3개만 존재).
프롬프트가 **150개 이상**, 소스는 메모장 / 카톡·슬랙 / 예전 LLM 대화기록 — **전부 비정형**.
현재 등록 흐름은 모달 → 제목 → 카테고리(필수) → 태그 → 본문. 150번 반복 불가.
→ **등록 마찰 제거가 1순위.** 이게 안 풀리면 나머지 기능은 빈 사전 위의 장식.

### 통증 아님 (확인됨)
- 변수 치환 — 번거롭다고 느끼지 않음. 우선순위 낮음
- 참고자료 — 실사용 0건(`_references` 키 미생성). 활성 코드에서 제외
- CSV 임포트 — 소스에 엑셀이 없음. 후순위

---

## 4. 성공 기준

| # | 기준 | 측정 방법 |
|---|---|---|
| S-1 | 원하는 프롬프트를 **10초 안에** 찾는다 | 팀원 3명에게 특정 프롬프트 찾기 요청 |
| S-2 | 팀원이 **남이 만든 프롬프트를 실제로 복사해 쓴다** | 복사 카운트 로그 |
| S-3 | 프롬프트 수정 후 **이전 버전으로 되돌릴 수 있다** | 기능 존재 여부 |
| S-4 | 데이터 손실 **0건** | 백업/복원 테스트 통과 |
| S-5 | 팀원이 설명 없이 **혼자 사용 시작** 가능 | 온보딩 문서 없이 첫 등록 성공 |
| S-6 | 프롬프트 1건 등록에 **30초 이내** | 실제 측정 |
| S-7 | 서버 배포 시점에 **실데이터 30건 이상** | 샘플 제외 개수 |

---

## 5. 사용자 스토리

### 필수 (MVP)

- 팀원으로서, **텍스트 덩어리를 붙여넣어 여러 개를 한 번에** 등록하고 싶다 *(P-4)*
- 팀원으로서, **제목과 본문만으로** 일단 저장하고 분류는 나중에 하고 싶다 *(P-4)*
- 팀원으로서, **키워드나 초성으로 검색해서** 원하는 프롬프트를 바로 찾고 싶다
- 팀원으로서, 찾은 프롬프트를 **한 번의 클릭으로 복사**하고 싶다
- 팀원으로서, 새 프롬프트를 **등록하고 카테고리·태그로 분류**하고 싶다
- 팀원으로서, 프롬프트를 **수정한 뒤에도 이전 내용을 확인**하고 싶다
- 팀원으로서, **다른 팀원이 등록한 프롬프트를 보고** 쓰고 싶다
- 팀원으로서, 자주 쓰는 프롬프트를 **즐겨찾기**로 모아두고 싶다
- 팀장으로서, 데이터가 **날아가지 않는다는 확신**을 갖고 싶다

### 선택 (있으면 좋음)

- 팀원으로서, 프롬프트에 **메모(사용 팁·주의사항)** 를 남기고 싶다
- 팀원으로서, **어떤 모델용**인지 구분하고 싶다 (Claude / GPT / Gemini / NotebookLM)
- 팀원으로서, 실수로 지운 프롬프트를 **복구**하고 싶다
- 팀원으로서, `{{변수}}` 를 채워서 복사하고 싶다 *(우선순위 낮음)*
- 팀원으로서, **참고자료(기사·인사이트)** 를 함께 모아두고 싶다

---

## 6. 범위 밖 (안 만드는 것)

명시해두지 않으면 계속 커진다.

- ❌ LLM 직접 호출 / 프롬프트 실행 — 사전이지 실행기가 아님
- ❌ AI로 프롬프트 자동 생성·개선 제안
- ❌ 회사 전체 공개 / 외부 접근
- ❌ 프롬프트 평가·별점·댓글
- ❌ 승인 워크플로우 (팀 규모상 불필요)
- ❌ 모바일 전용 앱 (반응형 웹으로 충분)
- ❌ 실시간 동시 편집

---

## 7. 우선순위 (사용자 확정)

```
1. 로컬 기능 완성도
2. 데이터 안정성 (백업 / 손실 방지)
3. 서버 배포
4. 팀 공유 고급 기능  ← 대부분 범위 밖으로
```

**해석:**
서버 배포(3순위)만으로 P-2가 해결되므로, 4순위 항목은 P5에서 대부분 제거.
1·2순위가 곧 P0~P2이고 여기에 무게를 싣는다.

---

## 8. 우선순위 재배치 결과

기존 로드맵에서 바뀌는 것.

| 항목 | 기존 | 변경 | 이유 |
|---|---|---|---|
| **일괄 붙여넣기 등록** | 없음 | **P1 신설·최우선** | P-4 최대 병목 |
| **카테고리 필수 해제** | 없음 | **P1 신설** | 150건 등록 시 분류 결정 불가 |
| **빠른 등록(제목·본문만)** | 없음 | **P1 신설** | 등록 마찰 제거 |
| **제목 자동 제안** | 없음 | **P1 신설** | 150번 제목 짓기 회피 |
| 100개+ 성능 | P2 상위 | **P2 최우선** | 150개 확정 |
| CSV 파서 재작성 | P1 필수 | **후순위** | 소스에 엑셀 없음 |
| 참고자료 기능 | 유지 | **활성 코드에서 제외** | 실사용 0건 |
| 기존 데이터 마이그레이션 | P1/P3 | **삭제** | 실데이터 0건 |
| 변수 치환 | P2 최우선 | P2 후순위 | 실제 통증 아님 |
| **버전 이력 / 되돌리기** | P5 | **P2 최우선** | P-3 직결, 로컬에서 가능 |
| 검색 강화 (하이라이트·notes·태그관리) | P2 중간 | **P2 상위** | P-1 직결, 제품의 심장 |
| 100개+ 성능 | P2 후반 | **P2 상위** | 검색 체감 속도 = S-1 |
| 휴지통 | P2 | 유지 | 우선순위 2위(데이터 안정성) |
| 사용 통계 | P2 | 유지 | S-2 측정에 필요 |
| 공유/개인 구분 | P4 | 유지 (단순화) | 배지 표시 수준만 |
| 관리자 화면·인기 대시보드 | P5 | **삭제** | 범위 밖 |
| 승인 워크플로우 | P5 | **삭제** | 범위 밖 |
| "내 사전으로 복사" | P5 | **삭제** | 전체 공유면 불필요 |

---

## 9. 핵심 설계 결정

### 공유 모델 — 기본 전체 공유
팀 내부 10명 규모에서 개인/공유를 세밀하게 나누면 관리 비용만 늘어난다.

- **모든 프롬프트는 팀 전체에 보임** (기본값)
- 작성자만 수정·삭제 가능 (소유자 표시)
- 개인 전용이 꼭 필요하면 `is_private` 플래그 하나로 처리

### 버전 이력 — 직전 3개 보관
전체 이력을 쌓으면 DB가 불필요하게 커진다.
수정 시 이전 내용을 스냅샷으로 저장하고, 3개를 넘으면 오래된 것부터 제거.
상세 화면에서 "이전 버전 보기 → 되돌리기".

### 자동정리 — P4 이후 검토 항목
프롬프트 본문만 붙여넣으면 제목·카테고리·태그·설명이 자동으로 채워지는 흐름.
**앱의 핵심 기능**이며 P-4(등록 마찰)를 정면으로 해결한다.

- 브라우저에서 API 키 직접 사용 불가 → **LiteLLM 프록시 경유, 서버 단계에서만 가능**
- 단건 등록과 일괄 등록이 **같은 엔진**을 쓴다 (입력 개수만 다름)
- **자동정리 결과는 반드시 사람이 확인 후 저장.** 자동 저장하지 않음
- 본문은 절대 수정하지 않는다. 메타데이터만 생성
- 세부 설계는 실사용 후 확정. 지금 확정하지 않는다

**P1에서 할 일:** 미리보기 화면을 제대로 만들고 **자동정리 버튼 자리만 비워둔다.**
나중에 AI를 붙일 때 화면은 그대로, 값 채우는 방식만 교체.

### 등록 — 최소 입력 원칙
**제목·본문만 필수.** 카테고리는 "미분류" 기본값, 태그·설명·메모는 접힌 영역.
일괄 붙여넣기 화면을 별도 제공(구분자 분할 → 미리보기 → 일괄 등록).
제목 미입력 시 본문 첫 줄에서 자동 제안.

### 성능 — 연산은 전부 브라우저에서 ★ 서버 사양 제약
서버 PC 사양이 넉넉하지 않고 이미 서비스 7개가 구동 중.
프롬프트 150개 ≈ 300KB이므로 **접속 시 전체를 한 번 내려받고**
검색·필터·정렬·diff·초성변환을 전부 클라이언트에서 처리한다.
서버는 저장 / 불러오기 / 인증만 담당. 500개까지 이 구조 유지 가능.

**금지:** 임베딩 기반 시맨틱 검색, SSR 프레임워크, PostgreSQL, 상시 백그라운드 작업

### 삭제 — 소프트 삭제 30일
`deleted_at` 기록 후 목록에서 제외. 휴지통에서 복구 가능. 30일 후 정리.

---

### 데이터 이관 — 단계적 수집
150개 전량을 한 번에 옮기려 하면 또 미뤄진다. 소스별로 3회차 분할.

| 회차 | 소스 | 방식 |
|---|---|---|
| 1 | 메모장 · 텍스트 파일 | 일괄 붙여넣기로 대량 처리 |
| 2 | 카톡 · 슬랙 | 검색해서 복사 |
| 3 | 예전 LLM 대화기록 | 가장 오래 걸림. 틈틈이 |

**1회차만 끝나면 서버 배포 진행.** 전량 이관은 배포의 전제조건이 아니다.

---

## 10. 대안 검토 (2026-08 조사)

기존 도구로 대체 가능한지 확인한 결과.

| 도구 | 성격 | 판단 |
|---|---|---|
| Langfuse / Agenta / Braintrust | 개발자용 LLMOps, 트레이싱 중심 | ❌ 용도 다름 |
| **prompts.chat** (스타 143k) | 팀용 프롬프트 라이브러리, 자체호스팅 | ❌ 기능은 상위호환이나 **너무 무거움** |
| Tae4an/mcp-prompt-manager | MCP 서버, 퍼지검색·버전관리 | △ 개인용. 팀 공유 불가 |

**prompts.chat 제외 사유:** PostgreSQL + Node 24 + Next.js SSR 필요, OAuth 전제,
한국어 미지원, 투표·리더보드 등 불필요 기능 다수. 현 서버 사양에 부적합.

**조사에서 가져온 것**
- 버전 diff 표시 (Tae4an) → P2 버전 이력에 포함
- 변수 치환이 사실상 표준 기능 → 후순위에서 중위로 상향

**어느 도구에도 없어서 우리만 갖는 것**
1. **일괄 붙여넣기** — 카톡·LLM 대화 덩어리를 그대로 등록 (모두 "잘 정리된 프롬프트 입력"을 전제함)
2. **한글 초성 검색** — 전부 영어권 도구라 미지원
3. 기존 팀 허브·인증과 동일한 접근 경로

---

## 11. 다음 단계

1. 이 PRD 확정
2. P0(파일 분리 + 저장 계층 추상화) 착수
3. P1 이후 실데이터 1회차 수집 → 서버 배포
