# TODO — 프롬프트 전자사전

> 3단계 규칙 2단계 (Task List)
> 상위: [`PRD.md`](./PRD.md) 무엇을·왜 / [`PROGRESS.md`](./PROGRESS.md) 로드맵 / 이 문서 실행 단위

## 작업 분담

| 역할 | 담당 | 하는 일 |
|---|---|---|
| 설계·브레인스토밍 | Claude (채팅) | 태스크 분해, 구조 결정, 막힐 때 상담 |
| 코드 실행 | Claude Code | 태스크 1개씩 실제 수정 |
| 검수 | Codex | 커밋 전 코드 리뷰 |

**원칙**
- 태스크 **1개 = 커밋 1개**. 여러 개 묶지 않는다
- 각 태스크마다 "완료 조건"을 만족하는지 브라우저에서 직접 확인
- 되돌릴 수 있는 단위로만 진행. 확인 전에는 다음 태스크로 넘어가지 않는다
- 애매하면 코드 짜기 전에 채팅으로 먼저 상의

---

## T-000 · 백업 — ✅ 완료

- [x] **T-000** 전체 백업 완료 (키 5개: data / categories / favorites / theme / searchHistory)
- [x] 실데이터 0건 확인 — 저장된 3개는 전부 샘플 (AI PM 가이드 / 블로그 글쓰기 / 데이터 분석)
- [x] `promptDictionary_references` 미생성 — 참고자료 실사용 0건
- [x] 원본 HTML을 `legacy/` 에 보관

> **지킬 데이터가 없으므로** 하위 호환·ID 마이그레이션 태스크는 전부 삭제됨.
> P0/P1을 과감하게 진행해도 됨.

---

## P0 — 저장소 정리 및 구조 분리

목표: GitHub에서 diff를 읽을 수 있게 만들고, 서버 이식 비용을 미리 낮춘다.

- [x] **T-001** GitHub 저장소 `prompt-dict` 생성 (private)
      ※ 이름 변경 원하면 여기서 확정 (문서 3개의 표기도 함께 수정)
- [x] **T-002** 로컬 클론 후 `git config --local user.name` / `user.email` 설정
      ※ 공용 서버에서 `--global` 금지
- [x] **T-003** `PRD.md` / `PROGRESS.md` / `TODO.md` 커밋
- [x] **T-004** 원본 HTML을 `legacy/` 폴더에 그대로 커밋 (비교 기준점)
- [x] **T-005** 폴더 구조 생성
      ※ 실제 구조는 아래와 다름 — `data.js` 대신 기능별 11개 파일로 분할

```
prompt-dict/
├── index.html
├── css/style.css
├── js/
│   ├── storage.js      ← 저장 계층 (핵심)
│   ├── data.js         ← 상태 변수, 초기화
│   ├── prompts.js      ← 프롬프트 CRUD·렌더링
│   ├── ui.js           ← 모달·토스트·테마
│   └── main.js         ← DOMContentLoaded 진입점
├── legacy/
└── docs/
```

- [x] **T-006** CSS 전체를 `css/style.css` 로 이동, `<link>` 연결 — `173d388`
      완료 조건 충족: 분리분을 도로 합쳐 원본과 md5 일치 확인
- [x] **T-007** JS 분할, `<script>` 로 순서대로 로드 — 계획서 [`docs/T007-split-plan.md`](./docs/T007-split-plan.md)
      실제 로드 순서: `config` → `ui` → `storage` → `categories` → `prompts` → `tags`
      → `search-history` → `selection` → `thumbnail` → `import` → `main`
      완료 조건 충족: 콘솔 에러 0건, 헤드리스 실행 정상
  - [x] **T-007a** `config.js`(전역 상수·상태) + `main.js`(진입점) + 빈 파일 9개 + `<script>` 11개 — `3c85a0c`
  - [x] **T-007b** `ui.js` 4개 (`formatDate` `fallbackCopy` `showToast` `setupThemeToggle`) — `9d705ab`
  - [x] **T-007c** `storage.js` 7개 (저장·불러오기·내보내기) — `ba0d136`
  - [x] **T-007d** `categories.js` 11개 — `2336a7a`
  - [x] **T-007e-1** `prompts.js` 검색·정렬·렌더링 7개 — `ab8eb79`
  - [x] **T-007e-2** `prompts.js` 모달·폼 10개 — `e1dfdc4`
  - [x] **T-007f** `tags.js` 4 + `search-history.js` 7 — `ba042a7`
  - [x] **T-007g** `selection.js` 8 + `thumbnail.js` 9 — `9a0f0aa`
  - [x] **T-007h** `import.js` 6 (JSON/CSV/TXT 파서) — `1881063`
  - [x] **T-007i** 참고자료 기능 제거 (함수 14 + 전역 4 + 모달 2 + 버튼 + `main.js` 호출) — `e338d35`
        ※ 원래 "이관 대상에서 제외"였으나 **완전 제거**로 격상. 실사용 0건이고
        `legacy/original.html` 과 git 히스토리에 보존되므로 복원 가능
        ※ **CSS는 남겨둠** — 참고자료용 스타일 정리는 별도 태스크
      ※ 회귀 1건: T-007a에서 `DOMContentLoaded` 를 하나로 합쳤다가 예외 격리가
      사라짐. Codex 검수로 발견 → `71c81d2` 로 리스너 2개 복원
      ※ 검증: `tools/fnmap.py` 이름별 본문 md5 대조로 매 단계 차이 0. 함수 87 → 73
- [x] **T-008** `js/storage.js` 저장 계층 인터페이스 — `0a0d19e`

```js
const PromptStorage = {
  async getPrompts(),        async savePrompts(list),
  async getFavorites(),      async saveFavorites(set),
  async getCategories(),     async saveCategories(list),
  async getSearchHistory(),  async saveSearchHistory(list),
  async getSetting(k),       async setSetting(k, v),   // _theme 등 평문
  async removeAll()                                   // 전체 삭제 (T-115 왕복 테스트용)
};
```

> **최초안에서 바뀐 점** (T-008에서 확정)
> - 이름 `Storage` → `PromptStorage` (브라우저 내장 인터페이스 섀도잉 회피)
> - 단건 `savePrompt(p)` / `deletePrompt(id)` → **전량** `savePrompts(list)`
>   현재 저장 코드가 전부 컬렉션 통째 저장이라 단건 API는 P3에서 재검토
> - `getReferences` / `saveReference` / `deleteReference` 삭제 (T-007i에서 기능 제거)
> - `getSearchHistory` / `saveSearchHistory` / `removeAll` 추가
>
> 읽기 3상태(없음 `null` / 파싱 실패 `throw` / 정상), 읽기 실패 시 쓰기 차단,
> `removeAll` 은 가드 우회 → [`docs/devlog.md`](./docs/devlog.md) §4

- [x] **T-009** 호출부를 `PromptStorage` 경유로 교체
      ※ 계획서상 단일 태스크였으나 Codex 검수 지적을 반영하며 5단계로 나눠 진행
      ※ `initializeData` try/catch 부재도 함께 처리 / 손상 복구 UI는 T-115로 미룸
      — 근거 [`docs/decisions/2026-09-08.md`](./docs/decisions/2026-09-08.md) §6
  - [x] **T-009a** 어댑터 타입 검증·실패 규약 보강 — `63dd66e`
        `JSON.parse('null')` 이 "키 없음"으로 오인되던 구멍, 직렬화 실패 규약,
        `removeAll` 성공 시에만 가드 해제
  - [x] **T-009b** 읽기 경로 전환 (읽기 5건) — `e93567c`
        초기화 순서 재배치(읽기 전부 → 대입 → 첫 쓰기), 리스너1만 async,
        초기화 실패 시 렌더링 중단. **리스너2는 동기 유지**
  - [x] **T-009c-1** 진입점이 안전한 7곳 — `09b2896`
        스냅샷 롤백 패턴 확립, 필요한 것만 저장, 토스트를 저장 뒤로 이동
  - [x] **T-009c-2** 인라인 `onclick` 경유 5곳 — `7f4aebd`
        본문 try/catch 로 Promise 누출 차단, 제자리 변경 개별 복원
  - [x] **T-009c-3** `FileReader` 콜백 3곳 + `initializeData` 기본값 저장 2건 — `a11997d`
        기본값 저장 실패는 롤백 대상이 아니라 초기화 실패 → `throw`
- [x] **T-010** 죽은 저장 함수 제거 — `e4ed724`
      `loadFromLocalStorage` · `saveToLocalStorage` · `saveCategories` (storage.js)
      + `saveSearchHistory` (**search-history.js** — 최초안의 "기존 함수 7개" 목록 밖이라
      T-009c-3 시점에 범위를 정정해 포함시킴)
      완료 조건 충족: `grep -rn "localStorage\." js/` = **`LocalStorageAdapter` 내부 4건뿐**
      (`getItem` ×2, `setItem`, `removeItem`). 함수 73 → 69
- [x] **T-011** `CLAUDE.md` 작성 (프로젝트 개요·구조·금지사항) — `5937a5d`, 이후 계속 갱신
- [x] **T-012** `README.md` 작성 — `82ba427`
- [x] **T-013** Codex 검수 → 커밋 → **P0 완료**
      검수를 3회로 나눠 받았고, 지적 **9건을 전부 반영**했다.
  - [x] **1차** (T-007 패턴 확정 직후) — 회귀 1건 + 계획서 오류 2건
        `DOMContentLoaded` 병합으로 예외 격리 상실 → `71c81d2`
        계획서 정정 + `tools/fnmap.py` 신설 → `9169f33`
  - [x] **2차** (T-007·T-008 완료 후) — 지적 6건 → T-009a~c 에서 반영
        타입 검증 / 초기화 순서 / 저장 반환값 무시 / `FileReader` async /
        직렬화 실패 / `removeAll` 가드
  - [x] **3차** (T-009·T-010 완료 후) — F1·F2 → `31d181d`
        F1 읽기 실패 가드를 키 단위로 (`failedKeys`), 실패 토스트에 원인 키
        F2 전역 `unhandledrejection` 안전망
  - [x] **3차 후속** — 어댑터 쓰기 rejection 을 `false` 로 정규화 → `18e2f11`
        reject 시 호출부의 롤백 분기를 건너뛰어, 실패한 변경이 메모리에 남고
        이후 저장에 딸려 들어가던 문제
      ※ 세션에서는 뒤 두 건을 T-011 / T-011b 로 불렀으나, T-011 번호는
      이미 `CLAUDE.md` 작성에 쓰이고 있어 여기서는 T-013 하위로 기록한다

> ## ✅ P0 완료 (2026-09-08)
>
> | | 착수 전 | 완료 |
> |---|---|---|
> | 파일 | 단일 HTML 4,365줄 | `index.html` + CSS 1 + JS 11 + `tools/fnmap.py` |
> | 함수 | 87개 (전부 인라인) | **69개** (전부 `js/`) |
> | `localStorage` | 앱 전역 11곳 | **`LocalStorageAdapter` 내부 4건뿐** |
> | 저장 실패 | 무시 (성공 UI만 표시) | 스냅샷 롤백 + 실패 토스트 |
> | 데이터 손상 | 조용히 덮어씀 | 키 단위 잠금 + 원본 보관 |
>
> 커밋 범위 `173d388` ~ `18e2f11`.
> **다음은 P1 — 등록 마찰 제거가 최우선 (T-104 일괄 붙여넣기).**

---

## P1 — 등록 마찰 제거 + 보안

> PRD P-4(최대 병목) 대응. **이 단계 끝나면 실데이터 수집 1회차 시작.**

### 1-A · 등록 마찰 제거 ★ 최우선

- [x] **T-101** 카테고리 필수 검증 해제 — 기본값 "미분류" 추가 `bff0a25`
- [x] **T-102** 빠른 등록 모드 — 제목·본문만 노출, 나머지는 "자세히" 접힘 영역 `df2a71c`
- [x] **T-103** 제목 자동 제안 — 미입력 시 본문 첫 줄 또는 앞 30자 `97d6d9c`
- [x] **T-104** **일괄 붙여넣기 화면** ★ 핵심 — 5커밋으로 분할 진행
      - [x] **T-104f** `validateCategory` 폴백 `기타` → `미분류` (+ `import.js` 7곳) `8314078`
      - [x] **T-104a** `splitChunks()` 순수 함수 (`js/bulk.js` 신설) `9ca6513`
      - [x] **T-104b** 모달 + 진입점 + 구분자 개수 실시간 표시 `6f87bee`
      - [x] **T-104c+d** 미리보기 행 + 경고 배지 4종 `599f610`
      - [x] **T-104e** 일괄 저장 + 전량 롤백 + 카테고리 일괄 지정 `c62b1c8`
      ✅ 완료 조건 달성 — 메모장풍 CRLF 텍스트 **12개를 한 번에 등록**
      ※ f 를 먼저 한 이유: 일괄 등록의 기본 카테고리가 미분류라,
        나중에 하면 그 사이 커밋들이 `기타`/`미분류` 의미가 어긋난 채 쌓인다
      ※ c+d 를 합친 이유: 경고 배지가 행의 기본 체크 상태를 결정해 한 몸이다
- [x] **T-105** 미분류 프롬프트 일괄 분류 UI (선택 → 카테고리 지정) `35c0552`
- [x] **T-106** 사이드바에 "미분류" 필터 추가 `bff0a25`

### 1-B · 보안·데이터

- [x] **T-111** `escapeHtml()` 유틸 추가 `4f049b3`
- [x] **T-112** `createPromptCard()` 전 필드 이스케이프 (B1) `4e18def`
      ✅ 제목에 `<img src=x onerror=alert(1)>` 넣어도 실행 안 됨
      ※ 텍스트·속성 문맥만 닫혔다. 인라인 `onclick` 은 이스케이프로 막히지 않아
        T-113으로 넘겼다 (실측 — devlog §6 / decisions 09-09 §6)
- [x] **T-113** 인라인 `onclick` → 이벤트 위임 (B2) `3313b0f`
      ✅ 태그에 `'` 넣어도 필터 정상. 인라인 핸들러 0건
      ※ 검수 F3(인덱스 식별자)도 함께 해결 — 카테고리에 id 도입
- [x] **T-114** `exportData()` 확장 — 전체 키 포함, `version: '2.0'` (B3) `27a96da`
      ※ 하위 호환을 넣었다. "실데이터 0건이라 불필요"였으나, 이제 사용자가
        v1.0 백업을 갖고 있을 수 있다. 버전이 아니라 **필드 존재로 분기**한다
- [x] **T-115** 왕복 테스트 — 내보내기 → 데이터 삭제 → 불러오기 전량 복원 `f91ecd4`
      ✅ 메모리·저장소 전량 일치. 손상 복구 배너(F1 후속)도 함께
- [x] **T-116** ID를 `crypto.randomUUID()` 로 (B5). 마이그레이션 불필요 `39f1bc5`
- [ ] **T-117** `updatedAt` 추가 (B6)
- [ ] **T-118** `isFavorite` 제거, `favoriteIds` 단일화 (B7)
- [ ] **T-119** 검색 범위에 `notes` 포함 (B8)
- [ ] **T-120** 샘플 데이터 제거 (B12)
- [ ] **T-121** TXT 구분자 `---` → `===PROMPT===` (B16)
- [x] **T-122** 임포트 파서에 `notes` 필드 추가 (B14) `27a96da`
      ※ T-114 조사 중 같은 결함이라 함께 처리했다.
        `parseJSONFile` 이 `thumbnailImage` 도 버리고 있어 그것도 같이 고쳤다
- [ ] **T-123** `Esc` 전역 모달 닫기 (B17)
- [ ] **T-124** 검색 디바운스 200ms (B18)
- [ ] **T-125** 썸네일 노출을 체크박스 방식으로 (B15)

### 1-C · 검수

- [ ] **T-130** Codex 검수 → 커밋 → **P1 완료**

---

## ★ D1 — 실데이터 수집 1회차

> 코드 작업 아님. 하지만 **명시적 태스크로 관리.** 이게 밀리면 프로젝트가 멈춘다.

- [ ] **D-101** 메모장·텍스트 파일에서 프롬프트 긁어모아 한 파일로 합치기
- [ ] **D-102** 일괄 붙여넣기로 등록 (목표 30건 이상 → **S-7**)
- [ ] **D-103** 등록 1건에 걸린 시간 측정 (목표 30초 이내 → **S-6**)
- [ ] **D-104** 쓰면서 불편했던 점 메모 → P2 재작성 근거로 사용

> **여기서 나온 메모가 P2의 진짜 우선순위를 결정한다.**
> D1 끝나기 전에는 P2 태스크를 확정하지 않는다.

---

## P2 — 기능 보강 (D1 이후 재확정)

> ⚠️ **아래는 잠정안.** D1(실데이터 수집)에서 나온 불편 메모로 다시 쓴다.
> 지금 확정하면 또 가설 위에 쌓는 것.

**거의 확실히 필요 (150개 확정)**

- [ ] **T-201** 더미 프롬프트 150개 생성 스크립트
- [ ] **T-202** 렌더링 성능 개선 — 페이지네이션 또는 가상 스크롤
      완료 조건: 150개 상태에서 검색 입력 지연 체감 없음 → **S-1**
- [ ] **T-203** 검색어 하이라이트
- [ ] **T-204** 태그 관리 화면 (이름 변경 / 병합 / 삭제)
- [ ] **T-205** **한글 초성 검색** — `ㅍㄹㅍㅌ` → "프롬프트"
      검색어가 초성으로만 구성되면 초성 매칭, 아니면 일반 검색
      클라이언트 연산, 서버 부하 0. 조사한 어떤 도구에도 없는 기능

**PRD 통증 직결**

- [ ] **T-211** 버전 이력 — 수정 시 스냅샷(최대 3개) *(P-3)*
- [ ] **T-212** 상세 모달 "이전 버전 보기" + 되돌리기 → **S-3**
- [ ] **T-212b** 버전 diff 색상 표시 (jsdiff ~30KB, 클라이언트 연산)
- [ ] **T-213** 휴지통 — `deletedAt` 소프트 삭제 + 복구 화면

**보류 — D1 결과 보고 판단**

- [ ] 사용 통계 (`usageCount`, `lastUsedAt`) → S-2 측정용
- [ ] 정렬 추가 (자주 쓰는 순 / 최근 사용순 / 수정순)
- [ ] 모델 구분 필드
- [ ] 단축키 `Ctrl+K` / `Ctrl+Enter`
- [ ] 태그 정확 일치 필터 (B10)
- [ ] CSV 파서 재작성 (B13) — 소스에 엑셀 없어 후순위
- [ ] 변수 치환 `{{변수}}` — 조사 결과 사실상 표준 기능이라 중위로 상향

**UI 개선 — D1 1회차 실사용에서 발견** ✅ 완료

> 근거: **D1 1회차(2026-09-10, 33건 등록) 실사용.**
> 헤드리스 검증으로는 발견되지 않는 종류다 — 스크롤 거리와 반복 횟수는
> 실제로 손을 움직여 봐야 드러난다.

- [x] **T-208** 선택 모드 버튼을 검색·정렬 줄로 이동 `5634306`
      카드 그리드 아래에 있어 33장을 전부 지나쳐야 닿았다
      (사이드바에 있는 줄 알았으나 실제로는 그리드 아래였고, 마찰은 더 컸다)
      툴바는 같은 줄에 합치지 않고 아래 별도 줄 —
      합치면 `flex: 1` 검색창이 모드 전환마다 요동친다
- [x] **T-209** 사이드바 상하 분리 — 목록만 스크롤, 하단 고정 `5634306`
      고정: 카테고리 관리 · 파일 업로드
      즐겨찾기는 카테고리와 같은 **필터**라 스크롤 영역에 유지
      (고정 영역은 **동작**만 둔다)
- [x] **T-210** 새 프롬프트 · 일괄 붙여넣기 버튼 상단 이동
      ★ **커밋 `a404721`(T-211)에 섞여 들어갔다.** 커밋 메시지에 T-210 내용이
        없어 이력만으로는 추적되지 않는다. 푸시한 커밋은 다시 쓰지 않으므로 여기에 기록한다
      배치: **2행 — 만들기 줄을 검색 줄 위에**
      한 줄 5개는 기각. 1280에서 검색창이 **649 → 236px** 로 줄고 게다가 접힌다(실측)
      검색은 결과 바로 위에 붙어야 하고, 선택 모드 툴바가 아래로 나올 때
      만들기 줄이 밀리지 않아야 한다 (토글 시 상단 이동 0px 실측)
- [x] **T-211** 맨 위 · 맨 아래 스크롤 버튼 `a404721`
      숨김은 `display` 가 아니라 `visibility` —
      `display:none` 이면 ↓ 가 사라질 때 ↑ 가 그 자리로 내려와
      **커서 아래에서 버튼이 바뀐다** (자리 불변 602/602/602 실측)
      표시 기준은 뷰포트 절반. 스크롤 거리가 임계값 이하면 상자째 숨김
      P4 Hub FAB 자리를 `--fab-reserve: 88px` 로 예약 — 그때 이 값만 조정하면 된다
      함께 고친 제품 버그: `button { transition: all }` 이 `visibility` 까지
      전이시켜 **숨겨야 할 버튼이 0.2초 동안 클릭 가능**했다
      ([devlog §5.9](docs/devlog.md))

**함께 잡힌 결함 3건** (T-208·T-209 커밋)

- [x] `setupCategoryButtons` 리스너 누적 — `.category-btn` 을 전역 조회해
      `#category-list` 밖의 두 버튼에 재렌더마다 리스너가 쌓였다
      (재렌더 5회 후 클릭 1번 → `applyFilters` 7회 → **1회**)
- [x] ⚙ 카테고리 관리 클릭이 `currentFilter` 를 `all` 로 초기화 —
      `data-category` 가 없는데 같은 리스너가 걸려 있었다.
      **분류 중 카테고리를 만들고 돌아오면 미분류 필터가 풀렸다 (T-105 루프에 직격)**
      → 쿼리를 `#category-list` 로 한정해 위 둘을 한 줄로 해소
- [x] `js/selection.js` 의 NUL 바이트 → `\u0000` 표기
      동작은 정상이었으나 grep 이 파일을 바이너리로 취급했다

**미채택 — 미분류 필터 시 자동으로 선택 모드 진입**

> 분류 흐름의 클릭을 하나 줄일 수 있지만, **의도하지 않은 상태에서
> [선택 삭제] 가 화면에 나타난다.** 되돌릴 수 없는 버튼이 사용자가
> 요청하지 않았는데 등장하는 것은 피한다 (T-105 설계 근거와 같은 이유).

**D1 2회차(이미지 소스) 준비 — [devlog §7](docs/devlog.md)**

> PPTX 33장 분석에서 나온 후속. 우선순위는 **D1 1회차 실사용 후 판단**한다.
> 늦어도 D1 2회차 착수 전에는 정리되어야 한다.

- [ ] **T-206** 일괄 붙여넣기 미리보기에 **"제목 중복" 경고** 추가
      지금은 본문 중복만 감지한다(T-104d). 첫 줄이 같은 문서 묶음에서는
      본문이 서로 다른데 **제목만 전부 같아지고, 화면에 아무 표시도 없다.**
      PPTX 슬라이드 1~10이 모두 `전체 디자인 설정:` 으로 수렴하는 것이 실제 사례
- [ ] **T-207** `suggestTitle` — 마크다운 강조 표기(`**` `__` `*` `_`) 제거 규칙
      불릿 제거 규칙과는 **별개 문제다.** `^[-*+]\s+` 는 `*` 뒤 공백을 요구하는데
      `**[AI Instruction: ...]` 에는 공백이 없어 걸리지 않고, 별표가 제목에 그대로 남는다
      (devlog §7 실측). 미리보기에서 손으로 고칠 수는 있으나 33개 반복은 마찰

- [ ] **T-230** Codex 전체 검수 → **P2 완료 / 로컬 완성**

---

## P3 — 서버 이식 준비

- [ ] **T-301** SQLite 스키마 확정 (`~/team-data/prompts.db`)
      ★ **한 프롬프트에 이미지가 여러 장일 수 있는지 이때 반드시 결정한다.**
      `thumbnail_path` 단일 컬럼으로는 부족할 수 있다 — 이미지가 부속물이 아니라
      본체인 소스가 있다([devlog §7](docs/devlog.md)). 나중에 정하면 마이그레이션이 된다.
      T-308(파일 저장 전환)·T-307(D1 수집분 이관)이 이 결정에 걸려 있다
- [ ] **T-302** FastAPI 프로젝트 뼈대 (`~/apps/prompt_dict/`)
- [ ] **T-303** `/api/prompts` CRUD 엔드포인트
- [ ] **T-304** `/api/categories`, `/api/versions`
- [ ] **T-305** `ApiAdapter` 구현
      ★ **인플라이트 가드를 어댑터 교체와 같은 커밋에 넣을 것.**
      `LocalStorageAdapter` 는 본문이 완전 동기라 `await` 가 마이크로태스크만 소비하므로
      경쟁 조건이 없었다. `fetch` 는 진짜 태스크 경계라 저장 중 다른 버튼 클릭이 처리되고,
      그러면 스냅샷 롤백이 **남의 변경까지 되돌린다.**
      (T-009 검수 F3 — 인덱스 기반 식별자도 이때 취약해진다. T-113에서 id 기반 전환 선행)
- [ ] **T-306** 어댑터 전환 스위치 (`config.js` 한 줄로 로컬/서버 선택)
- [ ] **T-307** 로컬 JSON → DB 마이그레이션 스크립트 (D1 수집분 이관용)
- [ ] **T-308** 썸네일 base64 → 파일 저장 + 경로 참조 (B4)
- [ ] **T-309** Codex 검수 → 커밋

---

## P4 — 서버 배포 (8807)

- [ ] **T-401** 포트 8807 확보 확인
- [ ] **T-402** `accounts.py` import 연결 (**수정 금지**)
- [ ] **T-403** 게이트 로그인 연동
- [ ] **T-404** 소유자 검증 가드 — 위키 `_banner_guard` 패턴, 전 엔드포인트 이중 방어
- [ ] **T-405** `REQUIRE_AUTH` 롤백 스위치
- [ ] **T-406** 소유자 표시 배지 UI
- [ ] **T-407** FAB "🏠 Hub" 버튼 삽입
- [ ] **T-407b** AI 자동정리 연결 — LiteLLM 프록시 경유, haiku
      - 기존 카테고리·태그 목록 주입 (태그 폭발 방지)
      - JSON만 반환받고 본문은 클라이언트 원본 사용
      - 개수 불일치·파싱 실패 시 전체 폐기 후 폴백 (조용한 실패 금지)
      - 제안값은 미리보기에 배지 표시, **사람 확인 후 저장**
- [ ] **T-408** 허브 카드 등록 + `_safe_next` 화이트리스트 추가
- [ ] **T-409** systemd 등록 (`stt.service` 템플릿)
- [ ] **T-410** `prompts.db` 4시 자동 백업 대상 추가
- [ ] **T-411** `00_서버_전체_지도.md` 갱신
- [ ] **T-412** 실데이터 마이그레이션 실행
- [ ] **T-413** 팀원 1명 테스트 → **S-5 확인**

---

## P5 — 배포 후 정리

- [ ] **T-501** 사용 통계 서버 집계 (S-2)
- [ ] **T-502** 운영 1개월 회고 → 안 쓰는 기능 제거

---

## Codex 검수 요청 템플릿

```
prompt-dict 프로젝트의 [T-1xx] 작업을 검수해줘.

변경 내용: (요약)
변경 파일: (목록)

중점 확인:
1. innerHTML 사용처에 escapeHtml 누락 없는지
2. localStorage 직접 호출이 storage.js 밖에 있는지
3. 기존 데이터와의 하위 호환이 깨지는지
4. 조용히 실패하는 경로(silent failure)가 있는지
5. 되돌릴 수 없는 데이터 변경이 있는지

문제가 있으면 파일:줄번호와 함께 알려줘.
```

※ 5번은 이 프로젝트에서 특히 중요. 과거 프로젝트에서 silent failure가 반복 발생했음.
