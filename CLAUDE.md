# CLAUDE.md — prompt-dict

이 파일은 Claude Code가 세션 시작 시 자동으로 읽습니다.

## 프로젝트

교육지원팀 내부용 **프롬프트 전자사전**. 흩어진 프롬프트를 모아 검색·공유·개선이력 관리.
기존 단일 HTML(4,365줄)을 구조 분리 → 로컬 완성 → 팀 서버(8807) 배포.

**문서 우선순위:** `PRD.md`(무엇을·왜) → `PROGRESS.md`(로드맵) → `TODO.md`(실행 단위)

## 작업 규칙 ★

- **태스크 1개 = 커밋 1개.** 여러 태스크를 묶어서 진행하지 않는다
- 한 태스크가 끝나면 **멈추고 요약만 보고**한다. 사용자가 브라우저로 확인 후 다음 지시
- 지시받지 않은 파일은 건드리지 않는다
- 되돌릴 수 있는 단위로만 변경한다
- 애매하면 코드를 짜기 전에 **먼저 질문**한다
- 한국어로 소통한다
- 검수는 Codex가 담당. 커밋 전 Codex 리뷰를 거친다

## 절대 금지

- **프롬프트 본문(`content`)을 임의로 수정·정리하지 않는다.** 원문 그대로 보존.
  프롬프트는 한 글자가 결과를 바꾼다. AI 자동정리는 메타데이터(제목·분류·태그)만
- **조용한 실패 금지.** 실패는 명시적으로 드러낸다. 임의 기본값으로 덮지 않는다
- `localStorage` 직접 호출 금지 (P0 이후). 반드시 `Storage` 어댑터 경유
- 서버 배포 시 `~/apps/team_accounts/accounts.py` 수정 금지 (공유 모듈, import만)
- git `--global` 설정 금지 (공용 서버). `--local`만 사용

## 성능 제약 ★

서버 PC 사양이 낮고 서비스 7개가 이미 구동 중.

- 검색·필터·정렬·diff·초성변환은 **전부 클라이언트**에서
- 서버는 저장 / 불러오기 / 인증만
- 금지: 임베딩 시맨틱 검색, SSR 프레임워크, PostgreSQL, 상시 백그라운드 작업
- DB는 SQLite

## 구조

```
index.html
css/style.css
js/storage.js   ← 저장 계층. 로컬=LocalStorageAdapter, 서버=ApiAdapter
js/data.js      ← 상태 변수, 초기화
js/prompts.js   ← 프롬프트 CRUD·렌더링
js/ui.js        ← 모달·토스트·테마·escapeHtml
js/main.js      ← DOMContentLoaded 진입점
legacy/         ← 원본 HTML (수정 금지, 비교용)
```

로컬↔서버 차이는 `storage.js` 어댑터 + 인증 + FAB 버튼 세 군데뿐.
**파일을 통째로 복사한 별도 서버 버전을 만들지 않는다.**

## 현재 상태

- 실데이터 0건 (샘플 3개뿐) → 하위 호환·마이그레이션 불필요
- 참고자료 기능 실사용 0건 → 활성 코드에서 제외 (legacy에 보존)
- 이관 대상 프롬프트 150개 이상, 소스는 메모장 / 카톡 / LLM 대화기록
- **최대 병목은 코드가 아니라 등록 마찰** — P1의 일괄 붙여넣기가 핵심

## 알려진 버그 (TODO.md 참조)

B1 카드 XSS · B2 태그 따옴표로 onclick 깨짐 · B3 내보내기 누락 ·
B13 CSV 단순 split · B14 임포트 notes 소실 · B15 썸네일 카테고리 하드코딩 · 외 12건

## 서버 배포 시 (P4)

- 포트 8807
- `accounts.py` 인증 + 게이트 로그인
- 소유자 검증 가드는 위키 `_banner_guard` 패턴 재사용 (이중 방어)
- FAB "🏠 Hub" 버튼 + 허브 카드 등록 + `_safe_next` 화이트리스트
- systemd 등록 (`stt.service` 템플릿)
- `prompts.db` 를 4시 자동 백업 대상에 추가
